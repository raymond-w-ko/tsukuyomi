(ns tsukuyomi.core)

(def first
  (fn [coll]
    (_emit_ "coll:first()")))

(def rest
  (fn [coll]
    (_emit_ "coll:rest()")))

(def conj
  (fn [coll x]
    (_emit_ "coll:conj(x)")))

(def cons
  (fn [x seq]
    (_emit_ "seq:cons(x)")))

(def second
  (fn [cell]
    (first (rest cell))))
